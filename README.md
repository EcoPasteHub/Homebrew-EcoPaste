1. 添加 EcoPaste 的 tap 源：

```bash
brew tap ayangweb/EcoPaste
```

2. 安装：

```bash
brew install --cask ecopaste
```

3. 更新：

```bash
brew upgrade --cask ecopaste
```

4. 卸载：

```bash
brew uninstall --cask ecopaste

brew untap ayangweb/EcoPaste
```
